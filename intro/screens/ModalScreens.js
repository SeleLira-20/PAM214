import { Text, StyleSheet, View } from 'react-native'

export default function ModalScreens () {
  
    return (
      <View>
        <Text> Proximamente by Yael & equipo </Text>
      </View>
    )
  }
const styles = StyleSheet.create({})