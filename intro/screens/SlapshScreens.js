import { Text, StyleSheet, View } from 'react-native'

export default function SlapshScreens () {
  
    return (
      <View>
        <Text> Proximamente by Karla Sofia & equipo </Text>
      </View>
    )
  }
const styles = StyleSheet.create({})
