import { Text, StyleSheet, View } from 'react-native'

export default function ScrollViewScreens () {
  
    return (
      <View>
        <Text> Proximamente by Isac Alvarez & equipo </Text>
      </View>
    )
  }
const styles = StyleSheet.create({})