import { Text, StyleSheet, View } from 'react-native'

export default function BottomSheetScreens () {
  
    return (
      <View>
        <Text> Proximamente by Valeria o Isabella & equipo </Text>
      </View>
    )
  }
const styles = StyleSheet.create({})