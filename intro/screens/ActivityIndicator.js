import { Text, StyleSheet, View } from 'react-native'

export default function ActivityIndicator () {
  
    return (
      <View>
        <Text> Proximamente by Israel & equipo </Text>
      </View>
    )
  }
const styles = StyleSheet.create({})