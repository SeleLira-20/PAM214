import { Text, StyleSheet, View } from 'react-native'

export default function FlatListScreens () {
  
    return (
      <View>
        <Text> Proximamente by Carlos & equipo </Text>
      </View>
    )
  }
const styles = StyleSheet.create({})