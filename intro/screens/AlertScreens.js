import { Text, StyleSheet, View } from 'react-native'

export default function AlertScreens () {
  
    return (
      <View>
        <Text> Proximamente by Santiago & equipo </Text>
      </View>
    )
  }
const styles = StyleSheet.create({})